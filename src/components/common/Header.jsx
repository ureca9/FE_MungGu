const Header = () => {
  return (
    <header className="fixed top-0 left-0 z-10 w-full border-b border-gray-300 backdrop-blur-md">
      <a className="container flex items-center justify-center h-16 mx-auto LogoFont text-[#3288FF] no-underline text-3xl">
        멍티비티
      </a>
    </header>
  );
};

export default Header;
